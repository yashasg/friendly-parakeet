name: Squad CI

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled placeholder
        run: |
          echo "Squad CI is workflow_dispatch-only; primary CI workflows own validation."
