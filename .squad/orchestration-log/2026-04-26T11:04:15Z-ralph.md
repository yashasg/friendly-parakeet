# Ralph orchestration: issue #73 difficulty ownership

**Timestamp:** 2026-04-26T11:04:15Z  
**Issue:** #73 — Reconcile time-based difficulty ramp with song-driven beatmap spawning

## Assignment

Clarify whether song mode or the legacy time ramp owns difficulty, and make UI difficulty labels map to actual runtime behavior.

## Implementation pass

Ralph established:

- Song mode uses selected authored chart variants (`easy`, `medium`, `hard`).
- Chart variants define obstacle density, kinds, lane/shape choices, and readability.
- `difficulty_system()` skips the time ramp while `SongState.playing == true`.
- Freeplay/random spawning owns the elapsed-time ramp and is not TestFlight/App Store v1 scope.

## Coordinator follow-up

The coordinator removed stale speed-ramp/speed-bar references from current song-mode docs and HUD/prototype examples.

## Result

Ralph's difficulty-ownership pass was validated and approved by Kujan.
