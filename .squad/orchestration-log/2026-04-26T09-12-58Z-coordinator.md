# Coordinator — Orchestration: issue #56 vcpkg overlay cache keys

**Date:** 2026-04-26T09:12:58Z  
**Role:** Coordination & Validation  
**Issue:** #56 — CI cache keys omit vcpkg-overlay changes  
**Status:** ACCEPTED & POSTED

## Work Summary

Coordinator reviewed Ralph's workflow changes and verified that every vcpkg/CMake cache key hashing `vcpkg.json` now also hashes `vcpkg-overlay/**`.

Pre-existing dirty-worktree workflow changes from earlier issues were separated from this review scope. The #56-specific policy change preserves existing cache prefixes and restore keys while making overlay port edits invalidate exact cache matches.

## Validation

1. **YAML syntax:** all `.github/workflows/*.yml` parsed successfully
2. **Cache-key sanity:** every `hashFiles(...)` line containing `vcpkg.json` contains `vcpkg-overlay/**`
3. **Regression sanity:** no stale exact `hashFiles('CMakeLists.txt', 'vcpkg.json')` remains
4. **Diff hygiene:** `git diff --check -- .github/workflows`
5. **Review:** Kujan approved

## Outcome

✅ Patch accepted  
✅ GitHub issue comment posted  
→ Ready for merge
