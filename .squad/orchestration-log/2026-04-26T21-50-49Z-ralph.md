# Ralph orchestration: issue #76 FTUE/tutorial

**Timestamp:** 2026-04-26T21:50:49Z  
**Issue:** #76 - FTUE/tutorial is specified but not implemented

## Assignment

Implement the user-approved minimal first-run tutorial path with persisted completion state for TestFlight.

## Implementation pass

Ralph established:

- `SettingsState::ftue_run_count`, clamped 0..5 and serialized to `settings.json`.
- Fresh/default FTUE state of 0, with legacy settings missing the field loading as incomplete.
- `ftue_complete()` semantics where `1+` is complete/unlocked.
- `GamePhase::Tutorial` and `ActiveScreen::Tutorial`.
- Title Confirm routing to Tutorial for incomplete FTUE and Level Select once complete.
- Tutorial Confirm completion that marks FTUE complete, saves settings, clears input, and transitions to Level Select.
- Tutorial UI route/screen content and automated test-player auto-confirm support.

## Coordinator follow-up

The coordinator validated the implementation, reconfigured CMake so the new tutorial JSON is copied into build output, and verified focused plus full native tests.

## Result

Ralph's minimal FTUE implementation was validated and approved by Kujan.
