# Orchestration Log — Redfoot

**Timestamp:** 2026-04-27T22:30:13Z  
**Task:** UI ECS audit on branch user/yashasg/ecs_refactor  
**Status:** ✅ COMPLETED

## Scope

Read-only ECS compliance audit of UI/input systems after #312/#322 work.

## Verdict

**Mostly clean** — One P1 finding (overlay render dead code + live JSON), one P3 (JSON operator[] risk), one P3 (UIActiveCache lazy emplacement).

## Key Findings

**P1 — overlay_render:** ui_render_system.cpp line 350–358 checks `ovr.contains("overlay_color")` (flat key) but paused.json stores as `overlay.color` (nested). Condition always false — pause dim overlay has never rendered. Compounds hot-path JSON access violation (#322 policy). Fix: Extract overlay color at screen-load time into ctx POD (OverlayLayout struct).

**P3 — spawn_ui_elements:** el["animation"]["speed"].get<float>() uses operator[] on JSON references. Per SKILL guideline: use `.at()` inside try/catch instead. Not hot path (screen-load time) but malformed animation block would crash.

**P3 — UIActiveCache:** Lazily emplaced inside game loop (active_tag_system.cpp:20-21). Not a crash risk, but registry mutation during gameplay is code smell. Initialize alongside other ctx singletons in game_loop_init.

## Confirmed Clean (Post-#322)

- UIState pure data (no I/O methods)
- ensure_active_tags_synced moved to system cpp
- ui_render_system const registry
- HudLayout / LevelSelectLayout cached at screen-load
- O(1) element lookup via element_map
- gesture_routing_system: EventQueue → dispatcher
- hit_test_system: structural ActiveTag view
- ui_source_resolver: hashed dispatch, read-only
- spawn_ui_elements / destroy_ui_elements only on screen_changed

## Output

Decision written to `.squad/decisions/inbox/redfoot-ui-ecs-audit.md`

---

*End of orchestration log*
