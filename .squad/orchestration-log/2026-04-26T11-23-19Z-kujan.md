# Kujan orchestration: issue #74 pause/resume

**Timestamp:** 2026-04-26T11:23:19Z  
**Issue:** #74 - Specify pause/resume behavior for audio-driven gameplay

## Review

**Verdict:** APPROVED after one requested change.

Kujan verified:

- `MusicContext.paused` and `song_playback_system()` correctly distinguish paused, resumed, and stopped music states.
- `song_time`, current beat computation, and scheduler spawning freeze while paused.
- Gameplay systems are phase-gated while paused, including cleanup/miss processing, lifetime timers, and particle gravity after coordinator fixes.
- Resume requires explicit paused-active Confirm/Resume input and has no countdown/grace.
- `EventQueue` clearing prevents stale gameplay input from firing on resume.
- Focus-loss handling routes `Playing` to `Paused`, and focus regain does not auto-resume.
- Docs cover TestFlight interruption QA and desktop/WASM focus-loss proxy cases.

## Requested change

Kujan requested cleanup miss energy clamping use `ENERGY_DEPLETED_EPSILON` instead of a raw `< 0.0f` check.

## Resolution

The coordinator applied the cleanup clamp fix, added regression coverage, reran focused/full validation, and Kujan approved on re-review.

## Routing

GitHub issue #74 comment posted after approval.
