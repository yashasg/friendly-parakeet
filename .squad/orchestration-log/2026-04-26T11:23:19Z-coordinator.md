# Coordinator orchestration: issue #74 pause/resume

**Timestamp:** 2026-04-26T11:23:19Z  
**Issue:** #74 - Specify pause/resume behavior for audio-driven gameplay

## Review actions

- Inspected music lifecycle state in `MusicContext`, `song_playback_system()`, and `play_session.cpp`.
- Verified `song_time` and beat scheduling freeze outside `GamePhase::Playing`.
- Verified pause entry and paused UI input handling clears stale `EventQueue` contents.
- Verified focus loss while `Playing` transitions to `Paused` and focus regain has no auto-resume path.
- Checked docs for overclaims around iOS lifecycle behavior and kept device interruption handling as TestFlight QA/device verification.

## Coordinator changes

- Added pause guards to `cleanup_system()`, `lifetime_system()`, and `particle_system()`.
- Added regression tests for paused cleanup/miss processing, paused lifetime timers, paused particle gravity, and cleanup epsilon clamping.
- Updated `design-docs/architecture.md` to describe cleanup/effect systems as pause-guarded.
- Fixed cleanup miss energy clamping to use `ENERGY_DEPLETED_EPSILON` consistently after Kujan's review.

## Validation

- `git diff --check` on #74 relevant files passed.
- Native build passed for `shapeshifter` and `shapeshifter_tests`.
- Focused tests passed: 319 assertions in 130 test cases.
- Full native non-benchmark suite passed: 2511 assertions in 766 test cases.

## Review outcome

Kujan approved on re-review after the cleanup energy clamp fix.
