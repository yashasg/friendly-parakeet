# Orchestration: Keaton — Archetype Duplicate Audit

**Date:** 2026-04-27T23:41:31Z  
**Scope:** Read-only audit of `app/systems/` and `tests/` for duplicate entity construction patterns.

## Deliverables

- Decision record: `.squad/decisions/inbox/keaton-archetype-duplicate-audit.md`
- Five ranked recommendations:
  - **P0:** Test helpers (test_helpers.h) have color contract divergences; must migrate to canonical archetype path
  - **P1a:** Extract `create_obstacle_base` into `obstacle_archetypes.h` (pre-archetype setup)
  - **P1b:** Extract `create_player_entity` into `app/archetypes/player_archetype.h` (unifies production + test)
  - **P2:** Refactor `ui_button_spawner.h` internally (consolidate MenuButton pattern)
  - Non-recommendations: score popup (single site), particles (no production emitter), JSON UI (data-driven)

## Key Findings

- **P0 is blocking:** Tests hardcode color values divergent from production (HighBar, LanePush, shape_gate).
- **P1a enabler for P1b:** Both player and obstacle factories depend on stable base construction.
- **P2 is internal:** No new file needed; consolidate via private inline helper within existing header.
- Recommended migration order: P0→P1a→P1b→P2.

## Status

CLOSED — audit complete, findings in decisions.md inbox.
