# Orchestration Log — McManus

**Timestamp:** 2026-04-27T22:30:13Z  
**Task:** Gameplay ECS audit on branch user/yashasg/ecs_refactor  
**Status:** ✅ COMPLETED

## Scope

Read-only audit of gameplay systems:
- obstacle, song_state, rhythm components
- scoring, collision, beat_scheduler, obstacle_spawn, cleanup, miss_detection systems

## Verdict

**Mostly clean** — One P1 finding (signal wiring in component header), one P2 (Tier-1 EventQueue migration incomplete), one P3 (metadata/runtime state mixing documentation).

## Key Findings

**P1 — obstacle_counter.h:** Function wire_obstacle_counter() (signal wiring logic) living in component header. Should move to new app/systems/obstacle_counter_system.cpp. Keep ObstacleCounter data struct in header.

**P2 — EventQueue Tier-1:** decisions.md specifies full dispatcher migration with step 4 = "Remove EventQueue struct". Tier 2 (GoEvent, ButtonPressEvent) is fully migrated. Tier 1 remains incomplete: input_system still pushes raw inputs to EventQueue; gesture_routing_system/hit_test_system read from EventQueue before forwarding to dispatcher. Complete the migration and remove EventQueue.

**P3 — SongState/DifficultyConfig:** Mix metadata (immutable) with runtime state (mutable per-frame). No correctness issue, but document which fields are "set once at init" vs "mutated per-frame".

## Confirmed Clean

- scoring_system collect-then-remove (miss + hit passes)
- collision_system per-kind structural views
- miss_detection_system emplace during iteration
- cleanup_system static vector + two-pass destroy
- beat_scheduler_system — all state in ctx
- obstacle_spawn_system — RNGState in ctx
- entt::dispatcher in ctx, wired via input_dispatcher.cpp
- on_obstacle_destroy signal connect/disconnect in game_loop.cpp
- Burnout ECS surface removed cleanly

## Output

Decision written to `.squad/decisions/inbox/mcmanus-gameplay-ecs-audit.md`

---

*End of orchestration log*
