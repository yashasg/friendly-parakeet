# Coordinator orchestration: issue #73 difficulty ownership

**Timestamp:** 2026-04-26T11:04:15Z  
**Issue:** #73 — Reconcile time-based difficulty ramp with song-driven beatmap spawning

## Review actions

- Read Ralph's handoff and inspected runtime code/docs.
- Verified Level Select labels map to `LevelSelectState::DIFFICULTY_KEYS`.
- Verified `setup_play_session()` passes the selected difficulty key into beatmap loading.
- Verified `difficulty_system()` and `obstacle_spawn_system()` return early while `SongState.playing == true`.
- Searched docs for stale current-game speed-ramp and speed-bar references.

## Coordinator changes

- Updated `design-docs/game.md` so song mode uses authored chart progression and the time ramp is freeplay-only.
- Updated `design-docs/game-flow.md` to remove speed-bar HUD/state references from current gameplay.
- Updated `design-docs/architecture.md` to describe `DifficultyConfig` as a freeplay fallback singleton.
- Updated `design-docs/prototype.md` examples to stop showing speed-ramp HUD values.

## Validation

- `git diff --check` on changed #73 files passed.
- Native configure/build passed.
- Focused tests passed: 130 assertions in 56 test cases.
- Full native non-benchmark suite passed: 2487 assertions in 759 test cases.

## Review outcome

Kujan approved with no changes requested.
