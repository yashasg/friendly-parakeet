# Ralph orchestration: issue #114 total_notes

**Timestamp:** 2026-04-26T22:34:20Z  
**Issue:** #114 - SongResults total_notes is never populated

## Assignment

Populate `SongResults::total_notes` from the loaded beatmap and ensure Song Complete can display valid note totals.

## Implementation pass

Ralph's earlier implementation set `SongResults::total_notes` to `beatmap.beats.size()` during `setup_play_session()`.

## Coordinator follow-up

The coordinator found the production assignment was present but shipped song/difficulty coverage was missing, then added a regression for all 3 levels × 3 difficulties and the UI source resolver path.

## Result

The production fix and missing coverage were validated and approved by Kujan.
