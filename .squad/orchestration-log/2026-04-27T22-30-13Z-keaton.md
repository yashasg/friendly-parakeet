# Orchestration Log — Keaton

**Timestamp:** 2026-04-27T22:30:13Z  
**Task:** EnTT API leverage audit on branch user/yashasg/ecs_refactor  
**Status:** ✅ COMPLETED

## Scope

Read-only audit of EnTT API usage patterns across components/, systems/, tests/. Parallel with Keyser's structural audit.

## Verdict

**Mostly clean** — No UB, no unsafe iteration, no correctness bugs. Three P2 items (test_player entity stale refs, hand-rolled bitmask) plus one SKILL file correction.

## Key Findings

**P2 — TestPlayerAction.done_flags:** Hand-rolled `uint8_t` bitmask with 6 manual getters/setters. Should use `enum class ActionDoneBit : uint8_t` with `_entt_enum_as_bitmask` — consistent with GamePhaseBit pattern.

**P2 — TestPlayerState.planned[]:** Raw entity array (entt::entity planned[MAX_PLANNED]) with no validation at storage time. Use sites apply `.valid()` guards. Consider wrapping or documenting "caller must validate" contract.

**P3 — UIState.current:** std::string screen name (inconsistent with element_map entt::id_type). Minor; could use entt::hashed_string for transitions.

**P3 — UIState per-frame std::string alloc:** One per dynamic-text UI entity per frame. Move-assigned from optional, so no copies. Acceptable for UI layer.

**SKILL FILE CORRECTION:** `.squad/skills/ui-json-to-pod-layout/SKILL.md` incorrectly states "emplace is deprecated in v3". In v3.16.0, ctx().emplace<T>() is NOT deprecated — it uses try_emplace (insert-if-absent, idempotent).

## Confirmed Clean

- Collect-then-remove pattern (cleanup, lifetime, scoring)
- entt::exclude<> structural views (collision_system)
- ctx().find<T>() null-guards
- entt::hashed_string usage
- GamePhaseBit / enum_as_bitmask
- Dispatcher drain (no-replay)
- Tag components (empty structs)
- Signal/sinks registration

## Output

Decision written to `.squad/decisions/inbox/keaton-entt-api-audit.md`

---

*End of orchestration log*
