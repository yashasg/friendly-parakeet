# Ralph orchestration: issue #74 pause/resume

**Timestamp:** 2026-04-26T11:23:19Z  
**Issue:** #74 - Specify pause/resume behavior for audio-driven gameplay

## Assignment

Define and implement pause/resume behavior for audio-driven gameplay, including music pause, `song_time` freeze, explicit resume semantics, and background/focus-loss QA expectations.

## Implementation pass

Ralph established:

- `MusicContext.paused` to distinguish paused streams from stopped streams.
- `song_playback_system()` lifecycle handling for play, pause, resume, stop, and `song_time` advancement only in `GamePhase::Playing`.
- `beat_scheduler_system()` gating so beats do not spawn while paused.
- `game_state_system()` paused UI handling with explicit Confirm/Resume and stale input clearing.
- Documentation for pause timing, no-countdown resume, and TestFlight interruption QA.

## Coordinator follow-up

The coordinator found and fixed remaining paused simulation leaks in cleanup/miss processing, lifetime timers, and particle gravity, then added regression tests.

## Result

Ralph's pause/resume pass was tightened, validated, and approved by Kujan after one cleanup clamp consistency fix.
