# Orchestration Log — Baer

**Timestamp:** 2026-04-27T22:30:13Z  
**Task:** ECS test coverage audit on branch user/yashasg/ecs_refactor  
**Status:** ✅ COMPLETED

## Scope

Read-only audit against docs/entt/ — dispatcher contract, view mutation safety, component purity, ctx singleton init, UI layout/cache, hashed strings, enum bitmasks, signal lifecycle, CI platform guards.

## Verdict

**Coverage gaps** — High-signal gaps only. All "mostly covered" areas are noted under Coverage to Preserve.

## Key Findings

| Priority | Guarantee | Gap | Recommended Test | Suggested Owner |
|----------|-----------|-----|------------------|-----------------|
| **P1** | R7 — Stale event discard | Events queued before phase transition survive in dispatcher if next frame re-enters same phase | Enqueue GoEvent → trigger phase transition → verify no GoEvent delivered in post-transition frame | Baer |
| **P1** | entt::dispatcher in ctx required by 8 production systems | test_components.cpp only asserts 6 of ~17 singletons — dispatcher is NOT listed | Add dispatcher to singleton assertion; add null-registry crash-guard test | Baer |
| **P2** | collect-then-remove only safe pattern | miss_detection_system emplaces during exclude<ScoredTag> iteration — no test verifies idempotency | Run miss_detection on N obstacles; verify count of MissTag == N and ScoredTag == N; run again, verify no second tag | Baer |
| **P2** | make_registry() canonical init contract | ~11 singletons (SongState, EnergyState, BeatMap, etc.) not checked | Extend singleton test to call ctx().get<T>() for every singleton, or report missing by name | Baer |
| **P3** | on_construct<> signal lifecycle | Tests entirely within #ifdef PLATFORM_DESKTOP — invisible on Linux CI | Port at least one on_construct<ObstacleTag> test to non-platform-gated file | Baer |

## Coverage That Must Be Preserved

- test_entt_dispatcher_contract.cpp — enqueue/update deferred semantics; drain = no-replay
- test_input_pipeline_behavior.cpp — Same-frame delivery; #213 sub-tick no-replay
- test_event_queue.cpp — GoEvent/ButtonPressEvent through dispatcher
- test_components.cpp [phase_mask] — GamePhaseBit enum_as_bitmask sentinel
- test_pr43_regression.cpp on_obstacle_destroy — pool-insertion-order safety
- test_lifecycle.cpp — ctx singleton null/missing returns false
- test_ui_element_schema.cpp — _hs literal hashed dispatch
- test_high_score_persistence.cpp — FNV-1a collision-free for 9 shipped keys
- test_world_systems.cpp [#242] cleanup — collect-then-remove safety

## Flaky / Platform Risks

1. #ifdef PLATFORM_DESKTOP — 12 tests in test_test_player_system.cpp invisible on Linux CI
2. WASM has zero tests — shapeshifter_tests excluded for Emscripten
3. test_pr43_regression.cpp #ifndef PLATFORM_WEB gate — one test skipped on WASM
4. Static buffer in cleanup_system.cpp — reused across calls; data race risk if parallelized
5. test_beat_log_system.cpp #ifdef _WIN32 — path separator handling conditional; macOS CI blind

## Output

Decision written to `.squad/decisions/inbox/baer-ecs-test-coverage-audit.md`

---

*End of orchestration log*
