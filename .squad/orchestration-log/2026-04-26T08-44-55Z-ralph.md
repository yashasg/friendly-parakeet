# Orchestration Log: Ralph (2026-04-26T08:44:55Z)

## Assignment

**Issue:** #46 — Release Workflows  
**Agent:** Ralph (background, claude-haiku-4.5)  
**Task:** Implement GitHub release workflow stubs for push-to-main and insider-branch releases.

## Execution

Ralph implemented release workflow scaffolding:
- `.github/workflows/squad-release.yml`: push-to-main workflow with build/test gates, GitHub release creation
- `.github/workflows/squad-insider-release.yml`: insider-branch counterpart with prerelease tag and version suffix

## Handoff

Ralph completed initial implementation. Coordinator performed tightening:
- Removed broad `|| echo` fallback from release creation step (explicit error propagation)
- Aligned checkout/cache actions to project CI pattern
- Validated YAML syntax and sanity assertions (build command, test command, release artifact path, no stubs)
- Posted GitHub issue comment with final validation summary

**Outcome:** ✅ ACCEPTED. Workflows ready for Kujan final review.

## Details

**Workflow structure (both):**
1. Checkout + cache build dir
2. Install Linux dependencies (libx11-dev, libxrandr-dev, libxinerama-dev, libxcursor-dev, libxi-dev, libxext-dev, libxrandr-dev)
3. Set VCPKG_ROOT environment variable
4. Run `./build.sh Release`
5. Run `./build/shapeshifter_tests "~[bench]"` (non-benchmark tests)
6. Create GitHub release with `build/shapeshifter` as sole artifact (existing tags skipped)

**Sanity checks passed:**
- YAML valid (Ruby/Psych parse)
- No TODO/stub markers
- No `|| echo` fallback in release creation
- Checkout/cache actions match CI pattern
- Build command correct (`./build.sh Release`)
- Test command excludes benchmarks (`"~[bench]"`)
- Artifact path correct (`build/shapeshifter`)
