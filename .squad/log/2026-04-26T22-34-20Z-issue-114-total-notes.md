# Issue #114: SongResults total_notes coverage

**Timestamp:** 2026-04-26T22:34:20Z  
**Squad:** Ralph (initial fix), Coordinator (tightening/validation), Kujan (review), Scribe (logging)

## Scope

Tightened issue #114 after finding that `SongResults::total_notes` was populated in production but the requested shipped song/difficulty regression coverage was missing.

## Outcome

- Added a play-session regression covering all shipped levels and all three difficulties.
- Each case enters `setup_play_session()`, verifies the selected beatmap loaded, and checks `SongResults.total_notes == beatmap.beats.size()`.
- The test also verifies `resolve_ui_int_source("SongResults.total_notes")` returns the same value for Song Complete UI stats.

## Validation

- `git diff --check` passed for the #114 files.
- Focused tests passed: 80 assertions in 17 test cases.
- Full native non-benchmark suite passed: 2619 assertions in 788 test cases.

## Review

Kujan approved with no blocking findings.

## GitHub

Follow-up comment posted to issue #114.
