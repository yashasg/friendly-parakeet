# Fenton — History

## Core Context

- **Project:** A C++20 raylib/EnTT rhythm bullet hell game with song-driven obstacles and shape matching synced to music.
- **Role:** Audio Expert
- **Joined:** 2026-05-01T00:00:00Z

## Learnings

- Pending first assignment.

- 2026-05-09T00:16:15.582-07:00 — Timing-drift experiment on `2_drama` medium/pro confirmed collision drift is primarily fixed-frame quantization: fresh combined runs mean `actual - expected` = 8.94 ms over 159 collisions versus 8.33 ms half-frame at 60 Hz; provided full log matched at 8.59 ms over 151 collisions. Artifacts: `/Users/yashasgujjar/.copilot/session-state/c0ddd445-5e34-4aa9-bc53-563866a0574f/files/timing-drift-experiment-2026-05-09/fenton-timing-drift-report.md`, `timing_drift_analysis.json`, `collision_drifts.csv`, and fresh run logs `run2_session_pro_rt0000000364_n0001.log` / `run3_session_pro_rt0000000322_n0001.log`.

- 2026-05-09T00:29:44.825-07:00 — Compared Reddit OP's STFT/onset/tempogram/subdivision pipeline against our 2_drama path. Key learning: our current pipeline already uses STFT flux plus tempogram-derived tempo, but still uses `librosa.beat.beat_track` for authoritative beat timestamps; naive PLP is not a drop-in replacement on 2_drama and should be evaluated as an augmenting candidate grid. Artifact: `/Users/yashasgujjar/.copilot/session-state/c0ddd445-5e34-4aa9-bc53-563866a0574f/files/beat-grid-tempogram-review-2026-05-09/fenton-op-pipeline-comparison.md`.

- 2026-05-09T00:41:48.960-07:00 — Produced Loop 1 DSP guardrails for Fenster to avoid overfitting subdivision snapping on `2_drama`. Confirmed analysis JSON has no explicit confidence/PLP fields; usable confidence proxies are event flux percentile, pass count, residual, class margin, and local repetition. Recommended quarter <=20 ms, eighth <=25 ms, and triplet <=25 ms only with strong repeated evidence. Artifacts: `/Users/yashasgujjar/.copilot/session-state/c0ddd445-5e34-4aa9-bc53-563866a0574f/files/loop1-dsp-guardrails-2026-05-09/fenton-loop1-dsp-guardrails.md`, `fenton_subdivision_guardrail_probe.py`, and `2_drama_guardrail_metrics.json`.

- 2026-05-09T13:14:28.264-07:00 — Onset-spike diagnostics review across shipped beatmaps (`1_stomper`, `2_drama`, `3_mental_corruption`) found current onset placement is timing-accurate (onset residual max 8–15 ms, strong negative lead bias) but under-utilized in two songs (onset share ~4.5–8.2% vs 14.5–28.0% for `3_mental_corruption`). All shipped events are labeled `downbeat` despite strong eighth-grid fit in diagnostics (e.g., `1_stomper` eighth p90 abs residual 12.8 ms), indicating subdivision coverage bottleneck rather than detector instability. Dedup appears stable (no candidate spacings <140 ms in current quarter-snap candidates), so next gains should come from wider/asymmetric residual windows and controlled subdivision expansion, not tighter suppression.

- 2026-05-10T02:36:13.250-07:00 — Audit of `tools/rhythm_pipeline.py`, librosa configs, analysis JSONs, diagnostics, and tests found four actionable issues filed as #400–#403. Key learnings: layer-aware merge is correct in `rhythm_pipeline.py`, but segment-focus selection can still collapse distinct cross-layer events after beat snapping; `onset_spike_smoke` artifacts are stale and still show `beat_fallback`; current loop1 diagnostics fail strict easy/medium IOI and dense-cluster gates; `test_get_audio_duration.py` is stale because the current librosa-only pipeline removed `get_audio_duration`/`FFPROBE_TIMEOUT`. Validation commands run: `python3 tools/test_layer_aware_pipeline.py` (pass), `python3 tools/test_validate_onset_spike_artifacts.py` (pass), `python3 tools/test_level_designer_onset_spike.py` (pass), `python3 tools/test_get_audio_duration.py` (import failure), and strict validation over `tools/diagnostics/*_loop1` plus `onset_spike_smoke` (failures captured in issues).

- 2026-05-10T02:40:52.785-07:00 — Fixed #400–#402 on `audit/autonomous-quality-loop`: segment-focus onset selection now applies difficulty IOI gating during selection while explicitly preserving protected cross-layer same-beat pairs, promotes sparse subdivision coverage for strict medium/hard diagnostics, and regenerated onset-spike smoke plus shipped loop1 diagnostics with zero `beat_fallback` rows. #403 was rechecked after Baer's clean handoff; `tools/test_get_audio_duration.py` is aligned with the current librosa duration path and passes.

- 2026-05-10T02:40:52.785-07:00 — Round 2 audit on `audit/autonomous-quality-loop-2` found and filed #415, #417, and #419. Key findings: `get_quiet_regions()` can emit adjacent duplicate QUIET/NOISY markers and unclosed trailing QUIET windows, causing outro/rest intensity misclassification; `validate_onset_spike_artifacts.py` still passes diagnostics containing `beat_fallback` rows despite the onset-only invariant; public analysis JSON still serializes raw instrument-named pass labels (`kick`, `snare`, `hihat`, `melody`) even though only broad classes (`percussive`, `harmonic`, `full-spectrum`) are approved. Python rhythm tests passed; strict loop2 content gates still fail on shipped beatmaps but same-beat validator false positives overlap existing validator issues #92/#226.

- 2026-05-10T18:00:00.000-07:00 — Round 3 audit on `audit/autonomous-quality-loop-3` (post PR #427). Filed #448 and #451 as regeneration residuals: shipped `content/beatmaps/*_analysis.json` still contain raw instrument-named pass labels (`kick`, `snare`, `hihat`, `melody`, `flux`) in `events[*].passes`, `onsets`, and `onset_diagnostics.raw_per_pass` even though #419 fixed the pipeline code (analysis artifacts were never regenerated); shipped `tools/diagnostics/*_loop1/onset_timing_events.csv` and the byte-identical `onset_spike_smoke/` mirror have zero `onset_class = harmonic` rows across all three songs while the source analysis JSONs carry 183/721/1498 harmonic events and a fresh `level_designer.py --experimental-onset-timing` regen produces 57 harmonic rows for `1_stomper` (loop1 diagnostics mtime 02:51 predates beatmap mtime 04:51). All shipped CSVs already satisfy `timing_source = onset` and only emit broad layers (no kick/snare leakage in CSV) and cross-layer 50 ms preservation in `merge_events` looks intact in code. No duplicates: existing open issues did not cover regenerated-artifact residuals.
