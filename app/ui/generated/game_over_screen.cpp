// Auto-generated by tools/rguilayout/codegen.py — DO NOT EDIT.
// Source: content/ui/screens/game_over.rgl
// Each control row in the .rgl becomes one ECS entity carrying a
// per-screen tag + per-kind tag + atomic UI components.
// Regenerate via `cmake --build` (codegen runs on .rgl change).

#include "components/actions.h"
#include "components/ui.h"
#include "tags/tags.h"

#include <entt/entt.hpp>

// Reference window from the .rgl: 720x1280 px.
void spawn_game_over_screen(entt::registry& reg) {
    {
        // TitleText (label)
        auto e = reg.create();
        reg.emplace<GameOverScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 210.0f, 440.0f);
        reg.emplace<UiBounds>(e, 300.0f, 60.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "GAME OVER");
    }
    {
        // ScoreLabel (label)
        auto e = reg.create();
        reg.emplace<GameOverScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 210.0f, 510.0f);
        reg.emplace<UiBounds>(e, 300.0f, 30.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "SCORE");
    }
    {
        // ScoreSlot (label)
        auto e = reg.create();
        reg.emplace<GameOverScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 210.0f, 540.0f);
        reg.emplace<UiBounds>(e, 300.0f, 46.0f);
        auto& label = reg.emplace<UiLabel>(e);
        (void)label;  // dynamic-text slot; bound by render system
    }
    {
        // HighScoreLabel (label)
        auto e = reg.create();
        reg.emplace<GameOverScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 210.0f, 600.0f);
        reg.emplace<UiBounds>(e, 300.0f, 30.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "HIGH SCORE");
    }
    {
        // HighScoreSlot (label)
        auto e = reg.create();
        reg.emplace<GameOverScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 210.0f, 634.0f);
        reg.emplace<UiBounds>(e, 300.0f, 32.0f);
        auto& label = reg.emplace<UiLabel>(e);
        (void)label;  // dynamic-text slot; bound by render system
    }
    {
        // NewBestSlot (label)
        auto e = reg.create();
        reg.emplace<GameOverScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 110.0f, 665.0f);
        reg.emplace<UiBounds>(e, 500.0f, 28.0f);
        auto& label = reg.emplace<UiLabel>(e);
        (void)label;  // dynamic-text slot; bound by render system
    }
    {
        // ReasonSlot (label)
        auto e = reg.create();
        reg.emplace<GameOverScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 110.0f, 685.0f);
        reg.emplace<UiBounds>(e, 500.0f, 40.0f);
        auto& label = reg.emplace<UiLabel>(e);
        (void)label;  // dynamic-text slot; bound by render system
    }
    {
        // PrevBestSlot (label)
        auto e = reg.create();
        reg.emplace<GameOverScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 110.0f, 712.0f);
        reg.emplace<UiBounds>(e, 500.0f, 24.0f);
        auto& label = reg.emplace<UiLabel>(e);
        (void)label;  // dynamic-text slot; bound by render system
    }
    {
        // ReasonSlotNewBest (label)
        auto e = reg.create();
        reg.emplace<GameOverScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 110.0f, 742.0f);
        reg.emplace<UiBounds>(e, 500.0f, 40.0f);
        auto& label = reg.emplace<UiLabel>(e);
        (void)label;  // dynamic-text slot; bound by render system
    }
    {
        // RestartButton (button)
        auto e = reg.create();
        reg.emplace<GameOverScreenTag>(e);
        reg.emplace<UiButtonTag>(e);
        reg.emplace<UiPosition>(e, 220.0f, 870.0f);
        reg.emplace<UiBounds>(e, 280.0f, 50.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "RESTART");
        reg.emplace<OnPress>(e, ActionId::RestartButton);
    }
    {
        // LevelSelectButton (button)
        auto e = reg.create();
        reg.emplace<GameOverScreenTag>(e);
        reg.emplace<UiButtonTag>(e);
        reg.emplace<UiPosition>(e, 220.0f, 935.0f);
        reg.emplace<UiBounds>(e, 280.0f, 50.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "LEVEL SELECT");
        reg.emplace<OnPress>(e, ActionId::LevelSelectButton);
    }
    {
        // MenuButton (button)
        auto e = reg.create();
        reg.emplace<GameOverScreenTag>(e);
        reg.emplace<UiButtonTag>(e);
        reg.emplace<UiPosition>(e, 220.0f, 1000.0f);
        reg.emplace<UiBounds>(e, 280.0f, 50.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "MAIN MENU");
        reg.emplace<OnPress>(e, ActionId::MenuButton);
    }
}

void despawn_game_over_screen(entt::registry& reg) {
    auto v = reg.view<GameOverScreenTag>();
    reg.destroy(v.begin(), v.end());
}
