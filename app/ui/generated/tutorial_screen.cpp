// Auto-generated by tools/rguilayout/codegen.py — DO NOT EDIT.
// Source: content/ui/screens/tutorial.rgl
// Each control row in the .rgl becomes one ECS entity carrying a
// per-screen tag + per-kind tag + atomic UI components.
// Regenerate via `cmake --build` (codegen runs on .rgl change).

#include "components/actions.h"
#include "components/ui.h"
#include "tags/tags.h"

#include <entt/entt.hpp>

// Reference window from the .rgl: 720x1280 px.
void spawn_tutorial_screen(entt::registry& reg) {
    {
        // TutorialTitle (label)
        auto e = reg.create();
        reg.emplace<TutorialScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 160.0f, 205.0f);
        reg.emplace<UiBounds>(e, 400.0f, 48.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "QUICK START");
    }
    {
        // SectionMatch (label)
        auto e = reg.create();
        reg.emplace<TutorialScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 160.0f, 333.0f);
        reg.emplace<UiBounds>(e, 400.0f, 40.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "1. MATCH THE SHAPE");
    }
    {
        // DemoCircleSlot (dummy rec)
        auto e = reg.create();
        reg.emplace<TutorialScreenTag>(e);
        reg.emplace<UiDummyRecTag>(e);
        reg.emplace<UiPosition>(e, 229.0f, 435.0f);
        reg.emplace<UiBounds>(e, 65.0f, 65.0f);
        auto& label = reg.emplace<UiLabel>(e);
        (void)label;  // dynamic-text slot; bound by render system
    }
    {
        // DemoSquareSlot (dummy rec)
        auto e = reg.create();
        reg.emplace<TutorialScreenTag>(e);
        reg.emplace<UiDummyRecTag>(e);
        reg.emplace<UiPosition>(e, 328.0f, 435.0f);
        reg.emplace<UiBounds>(e, 65.0f, 65.0f);
        auto& label = reg.emplace<UiLabel>(e);
        (void)label;  // dynamic-text slot; bound by render system
    }
    {
        // DemoTriangleSlot (dummy rec)
        auto e = reg.create();
        reg.emplace<TutorialScreenTag>(e);
        reg.emplace<UiDummyRecTag>(e);
        reg.emplace<UiPosition>(e, 427.0f, 435.0f);
        reg.emplace<UiBounds>(e, 65.0f, 65.0f);
        auto& label = reg.emplace<UiLabel>(e);
        (void)label;  // dynamic-text slot; bound by render system
    }
    {
        // MatchHint (label)
        auto e = reg.create();
        reg.emplace<TutorialScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 110.0f, 538.0f);
        reg.emplace<UiBounds>(e, 500.0f, 32.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "BECOME THE SHAPE BEFORE IT HITS YOU");
    }
    {
        // SectionDodge (label)
        auto e = reg.create();
        reg.emplace<TutorialScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 160.0f, 640.0f);
        reg.emplace<UiBounds>(e, 400.0f, 40.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "2. DODGE LANES");
    }
    {
        // DodgeHint (label)
        auto e = reg.create();
        reg.emplace<TutorialScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 110.0f, 710.0f);
        reg.emplace<UiBounds>(e, 500.0f, 32.0f);
        auto& label = reg.emplace<UiLabel>(e);
        (void)label;  // dynamic-text slot; bound by render system
    }
    {
        // SectionEnergy (label)
        auto e = reg.create();
        reg.emplace<TutorialScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 160.0f, 806.0f);
        reg.emplace<UiBounds>(e, 400.0f, 40.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "3. KEEP YOUR ENERGY");
    }
    {
        // EnergyHint (label)
        auto e = reg.create();
        reg.emplace<TutorialScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 110.0f, 877.0f);
        reg.emplace<UiBounds>(e, 500.0f, 32.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "MISSES DRAIN THE BAR ON THE LEFT");
    }
    {
        // ClearHint (label)
        auto e = reg.create();
        reg.emplace<TutorialScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 110.0f, 973.0f);
        reg.emplace<UiBounds>(e, 500.0f, 32.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "CLEAR A SONG TO SET A HIGH SCORE");
    }
    {
        // ContinueButton (button)
        auto e = reg.create();
        reg.emplace<TutorialScreenTag>(e);
        reg.emplace<UiButtonTag>(e);
        reg.emplace<UiPosition>(e, 180.0f, 1075.0f);
        reg.emplace<UiBounds>(e, 360.0f, 102.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "START");
        reg.emplace<OnPress>(e, ActionId::ContinueButton);
    }
}

void despawn_tutorial_screen(entt::registry& reg) {
    auto v = reg.view<TutorialScreenTag>();
    reg.destroy(v.begin(), v.end());
}
