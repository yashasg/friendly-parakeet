// Auto-generated by tools/rguilayout/codegen.py — DO NOT EDIT.
// Source: content/ui/screens/settings.rgl
// Each control row in the .rgl becomes one ECS entity carrying a
// per-screen tag + per-kind tag + atomic UI components.
// Regenerate via `cmake --build` (codegen runs on .rgl change).

#include "components/actions.h"
#include "components/ui.h"
#include "tags/tags.h"

#include <entt/entt.hpp>

// Reference window from the .rgl: 720x1280 px.
void spawn_settings_screen(entt::registry& reg) {
    {
        // SettingsTitle (label)
        auto e = reg.create();
        reg.emplace<SettingsScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 210.0f, 400.0f);
        reg.emplace<UiBounds>(e, 300.0f, 48.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "SETTINGS");
    }
    {
        // AudioOffsetLabel (label)
        auto e = reg.create();
        reg.emplace<SettingsScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 144.0f, 510.0f);
        reg.emplace<UiBounds>(e, 200.0f, 40.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "Audio Offset");
    }
    {
        // AudioOffsetMinus (button)
        auto e = reg.create();
        reg.emplace<SettingsScreenTag>(e);
        reg.emplace<UiButtonTag>(e);
        reg.emplace<UiPosition>(e, 180.0f, 560.0f);
        reg.emplace<UiBounds>(e, 72.0f, 77.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "-");
        reg.emplace<OnPress>(e, ActionId::AudioOffsetMinus);
    }
    {
        // AudioOffsetDisplay (label)
        auto e = reg.create();
        reg.emplace<SettingsScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 252.0f, 560.0f);
        reg.emplace<UiBounds>(e, 216.0f, 77.0f);
        auto& label = reg.emplace<UiLabel>(e);
        (void)label;  // dynamic-text slot; bound by render system
    }
    {
        // AudioOffsetPlus (button)
        auto e = reg.create();
        reg.emplace<SettingsScreenTag>(e);
        reg.emplace<UiButtonTag>(e);
        reg.emplace<UiPosition>(e, 468.0f, 560.0f);
        reg.emplace<UiBounds>(e, 72.0f, 77.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "+");
        reg.emplace<OnPress>(e, ActionId::AudioOffsetPlus);
    }
    {
        // HapticsToggle (button)
        auto e = reg.create();
        reg.emplace<SettingsScreenTag>(e);
        reg.emplace<UiButtonTag>(e);
        reg.emplace<UiPosition>(e, 152.0f, 720.0f);
        reg.emplace<UiBounds>(e, 416.0f, 100.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "HAPTICS: ON");
        reg.emplace<OnPress>(e, ActionId::HapticsToggle);
    }
    {
        // HapticsValue (label)
        auto e = reg.create();
        reg.emplace<SettingsScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 260.0f, 829.0f);
        reg.emplace<UiBounds>(e, 200.0f, 32.0f);
        auto& label = reg.emplace<UiLabel>(e);
        (void)label;  // dynamic-text slot; bound by render system
    }
    {
        // ReduceMotionToggle (button)
        auto e = reg.create();
        reg.emplace<SettingsScreenTag>(e);
        reg.emplace<UiButtonTag>(e);
        reg.emplace<UiPosition>(e, 152.0f, 880.0f);
        reg.emplace<UiBounds>(e, 416.0f, 100.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "MOTION: OFF");
        reg.emplace<OnPress>(e, ActionId::ReduceMotionToggle);
    }
    {
        // ReduceMotionValue (label)
        auto e = reg.create();
        reg.emplace<SettingsScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 260.0f, 989.0f);
        reg.emplace<UiBounds>(e, 200.0f, 32.0f);
        auto& label = reg.emplace<UiLabel>(e);
        (void)label;  // dynamic-text slot; bound by render system
    }
    {
        // CloseButton (button)
        auto e = reg.create();
        reg.emplace<SettingsScreenTag>(e);
        reg.emplace<UiButtonTag>(e);
        reg.emplace<UiPosition>(e, 260.0f, 1040.0f);
        reg.emplace<UiBounds>(e, 200.0f, 100.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "BACK");
        reg.emplace<OnPress>(e, ActionId::CloseButton);
    }
}

void despawn_settings_screen(entt::registry& reg) {
    auto v = reg.view<SettingsScreenTag>();
    reg.destroy(v.begin(), v.end());
}
