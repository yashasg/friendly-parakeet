#pragma once

// Compatibility header. Canonical camera entity data lives in entities/,
// and camera/render resource context types live in rendering/.
#include "../entities/camera_entity.h"
#include "../rendering/camera_resources.h"
