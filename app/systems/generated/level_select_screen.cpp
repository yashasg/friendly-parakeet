// Auto-generated by tools/rguilayout/codegen.py — DO NOT EDIT.
// Source: content/ui/screens/level_select.rgl
// Each control row in the .rgl becomes one ECS entity carrying a
// per-screen tag + per-kind tag + atomic UI components.
// Regenerate via `cmake --build` (codegen runs on .rgl change).

#include "components/actions.h"
#include "components/ui.h"
#include "tags/tags.h"

#include <entt/entt.hpp>

// Reference window from the .rgl: 720x1280 px.
void spawn_level_select_screen(entt::registry& reg) {
    {
        // HeaderText (label)
        auto e = reg.create();
        reg.emplace<LevelSelectScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 210.0f, 80.0f);
        reg.emplace<UiBounds>(e, 300.0f, 48.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "SELECT LEVEL");
    }
    {
        // StartButton (button)
        auto e = reg.create();
        reg.emplace<LevelSelectScreenTag>(e);
        reg.emplace<UiButtonTag>(e);
        reg.emplace<UiPosition>(e, 210.0f, 1050.0f);
        reg.emplace<UiBounds>(e, 300.0f, 60.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "START");
        reg.emplace<OnPress>(e, ActionId::StartButton);
    }
}

void despawn_level_select_screen(entt::registry& reg) {
    auto v = reg.view<LevelSelectScreenTag>();
    reg.destroy(v.begin(), v.end());
}
