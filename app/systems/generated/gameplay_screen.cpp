// Auto-generated by tools/rguilayout/codegen.py — DO NOT EDIT.
// Source: content/ui/screens/gameplay.rgl
// Each control row in the .rgl becomes one ECS entity carrying a
// per-screen tag + per-kind tag + atomic UI components.
// Regenerate via `cmake --build` (codegen runs on .rgl change).

#include "components/actions.h"
#include "components/ui.h"
#include "tags/tags.h"

#include <entt/entt.hpp>

// Reference window from the .rgl: 720x1280 px.
void spawn_gameplay_screen(entt::registry& reg) {
    {
        // ScoreSlot (label)
        auto e = reg.create();
        reg.emplace<GameplayHudTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 60.0f, 20.0f);
        reg.emplace<UiBounds>(e, 150.0f, 40.0f);
        auto& label = reg.emplace<UiLabel>(e);
        (void)label;  // dynamic-text slot; bound by render system
    }
    {
        // HighScoreSlot (label)
        auto e = reg.create();
        reg.emplace<GameplayHudTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 60.0f, 50.0f);
        reg.emplace<UiBounds>(e, 150.0f, 32.0f);
        auto& label = reg.emplace<UiLabel>(e);
        (void)label;  // dynamic-text slot; bound by render system
    }
    {
        // EnergyLabel (label)
        auto e = reg.create();
        reg.emplace<GameplayHudTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 10.0f, 745.0f);
        reg.emplace<UiBounds>(e, 120.0f, 32.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "ENERGY");
    }
    {
        // EnergyBarSlot (dummy rec)
        auto e = reg.create();
        reg.emplace<GameplayHudTag>(e);
        reg.emplace<UiDummyRecTag>(e);
        reg.emplace<UiPosition>(e, 10.0f, 790.0f);
        reg.emplace<UiBounds>(e, 30.0f, 200.0f);
        auto& label = reg.emplace<UiLabel>(e);
        (void)label;  // dynamic-text slot; bound by render system
    }
    {
        // LaneDividerSlot (dummy rec)
        auto e = reg.create();
        reg.emplace<GameplayHudTag>(e);
        reg.emplace<UiDummyRecTag>(e);
        reg.emplace<UiPosition>(e, 0.0f, 1120.0f);
        reg.emplace<UiBounds>(e, 720.0f, 4.0f);
        auto& label = reg.emplace<UiLabel>(e);
        (void)label;  // dynamic-text slot; bound by render system
    }
    {
        // ShapeButtonCircle (dummy rec)
        auto e = reg.create();
        reg.emplace<GameplayHudTag>(e);
        reg.emplace<UiDummyRecTag>(e);
        reg.emplace<LaneButtonTag>(e);
        reg.emplace<UiShapeIconCircleTag>(e);
        reg.emplace<UiPosition>(e, 130.0f, 1140.0f);
        reg.emplace<UiBounds>(e, 140.0f, 100.0f);
        auto& label = reg.emplace<UiLabel>(e);
        (void)label;  // dynamic-text slot; bound by render system
    }
    {
        // ShapeButtonSquare (dummy rec)
        auto e = reg.create();
        reg.emplace<GameplayHudTag>(e);
        reg.emplace<UiDummyRecTag>(e);
        reg.emplace<LaneButtonTag>(e);
        reg.emplace<UiShapeIconSquareTag>(e);
        reg.emplace<UiPosition>(e, 290.0f, 1140.0f);
        reg.emplace<UiBounds>(e, 140.0f, 100.0f);
        auto& label = reg.emplace<UiLabel>(e);
        (void)label;  // dynamic-text slot; bound by render system
    }
    {
        // ShapeButtonTriangle (dummy rec)
        auto e = reg.create();
        reg.emplace<GameplayHudTag>(e);
        reg.emplace<UiDummyRecTag>(e);
        reg.emplace<LaneButtonTag>(e);
        reg.emplace<UiShapeIconTriangleTag>(e);
        reg.emplace<UiPosition>(e, 450.0f, 1140.0f);
        reg.emplace<UiBounds>(e, 140.0f, 100.0f);
        auto& label = reg.emplace<UiLabel>(e);
        (void)label;  // dynamic-text slot; bound by render system
    }
    {
        // PauseButton (button)
        auto e = reg.create();
        reg.emplace<GameplayHudTag>(e);
        reg.emplace<UiButtonTag>(e);
        reg.emplace<UiPosition>(e, 620.0f, 10.0f);
        reg.emplace<UiBounds>(e, 80.0f, 50.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "||");
        reg.emplace<OnPress>(e, ActionId::PauseButton);
    }
    {
        // ChainSlot (label)
        auto e = reg.create();
        reg.emplace<GameplayHudTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 80.0f, 80.0f);
        reg.emplace<UiBounds>(e, 160.0f, 32.0f);
        auto& label = reg.emplace<UiLabel>(e);
        (void)label;  // dynamic-text slot; bound by render system
    }
}

void despawn_gameplay_screen(entt::registry& reg) {
    auto v = reg.view<GameplayHudTag>();
    reg.destroy(v.begin(), v.end());
}
