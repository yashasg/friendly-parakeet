// Auto-generated by tools/rguilayout/codegen.py — DO NOT EDIT.
// Source: content/ui/screens/paused.rgl
// Each control row in the .rgl becomes one ECS entity carrying a
// per-screen tag + per-kind tag + atomic UI components.
// Regenerate via `cmake --build` (codegen runs on .rgl change).

#include "components/ui.h"
#include "tags/tags.h"

#include <entt/entt.hpp>

// Reference window from the .rgl: 720x1280 px.
void spawn_paused_screen(entt::registry& reg) {
    {
        // PausedText (label)
        auto e = reg.create();
        reg.emplace<PausedScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 90.0f, 420.0f);
        reg.emplace<UiBounds>(e, 540.0f, 80.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "PAUSED");
    }
    {
        // ResumeInstruction (label)
        auto e = reg.create();
        reg.emplace<PausedScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 90.0f, 540.0f);
        reg.emplace<UiBounds>(e, 540.0f, 36.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "TAP RESUME TO CONTINUE");
    }
    {
        // ResumeButton (button)
        auto e = reg.create();
        reg.emplace<PausedScreenTag>(e);
        reg.emplace<UiButtonTag>(e);
        reg.emplace<UiPosition>(e, 160.0f, 620.0f);
        reg.emplace<UiBounds>(e, 400.0f, 100.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "RESUME");
        reg.emplace<UiActionResumeButtonTag>(e);
    }
    {
        // MenuInstruction (label)
        auto e = reg.create();
        reg.emplace<PausedScreenTag>(e);
        reg.emplace<UiLabelTag>(e);
        reg.emplace<UiPosition>(e, 90.0f, 760.0f);
        reg.emplace<UiBounds>(e, 540.0f, 36.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "OR RETURN TO MAIN MENU");
    }
    {
        // MenuButton (button)
        auto e = reg.create();
        reg.emplace<PausedScreenTag>(e);
        reg.emplace<UiButtonTag>(e);
        reg.emplace<UiPosition>(e, 160.0f, 820.0f);
        reg.emplace<UiBounds>(e, 400.0f, 100.0f);
        auto& label = reg.emplace<UiLabel>(e);
        ui_label_set(label, "MAIN MENU");
        reg.emplace<UiActionMenuButtonTag>(e);
    }
}

void despawn_paused_screen(entt::registry& reg) {
    auto v = reg.view<PausedScreenTag>();
    reg.destroy(v.begin(), v.end());
}
